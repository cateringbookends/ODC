{
  "name": "odc-event-dashboard",
  "version": "2.0.0",
  "description": "ODC Sales Event Dashboard — static front-end + Node.js/PostgreSQL server with live reload.",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
