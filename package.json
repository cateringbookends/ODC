{
  "name": "odc-event-dashboard",
  "version": "1.0.0",
  "description": "ODC Sales Event Dashboard — static front-end + zero-dependency Node/SQLite server with live reload.",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node smoke-test.js",
    "google:status": "node setup-google-sheets.js --status",
    "google:setup": "node setup-google-sheets.js",
    "google:sync": "node setup-google-sheets.js --sync"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
